import DocsBar from '@/sections/DocsBar';
export const Docs = () => (
	<DocsBar
		links={[
			{ name: '0.2', href: '/docs/0.2' },
			{ name: 'Projects', href: '/docs/0.2/project' },
			{ name: 'Images', href: '/docs/0.2/image' },
		]}
	/>
);

<Docs />

# TeXbld 0.2

(Under Development)

This release is expected to come with better error handling and some convenient utilities.
