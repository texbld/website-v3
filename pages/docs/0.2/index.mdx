import DocsBar from '@/sections/DocsBar';
export const Docs = () => (
	<DocsBar
		links={[
			{ name: '/0.2/', href: '/docs/0.2' },
			{ name: 'Projects', href: '/docs/0.2/project' },
			{ name: 'Images', href: '/docs/0.2/image' },
			{ name: 'Resources', href: '/docs/0.2/resource' },
			{ name: 'Sha256', href: '/docs/0.2/sha' },
		]}
	/>
);

<Docs />

# TeXbld 0.2

TeXbld 0.2 comes with a variety of new improvements, which include:

1. Image and Project Validation
2. Utilities for pegging revision and sha256 of GitHub repositories
3. Prettier error messages (instead of throwing raw python exceptions)